---
const currentYear = new Date().getFullYear();

const links = {
  product: [
    { label: 'Características', href: '#features' },
    { label: 'Beneficios', href: '#beneficios' },
    { label: 'Precios', href: '#precios' },
  ],
  company: [
    { label: 'Sobre Nosotros', href: '#' },
    { label: 'Blog', href: '#' },
    { label: 'Soporte', href: '#contacto' },
  ]
};
---

<footer class="border-t border-white/10 pt-16 pb-8">
  <div class="container-custom">
    <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-10 mb-12">
      <!-- Brand -->
      <div class="lg:col-span-2">
        <a href="/" class="flex items-center gap-3 mb-4">
          <div class="w-10 h-10 rounded-xl bg-gradient-to-br from-primary-500 to-primary-600 flex items-center justify-center shadow-lg">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M3 11v3a1 1 0 0 0 1 1h16a1 1 0 0 0 1-1v-3"/>
              <path d="M12 19H4a1 1 0 0 1-1-1v-2a1 1 0 0 1 1-1h16a1 1 0 0 1 1 1v2a1 1 0 0 1-1 1h-3.83"/>
              <path d="m3 11 7.77-6.04a2 2 0 0 1 2.46 0L21 11H3Z"/>
              <path d="M12.97 19.77 7 15h12.5l-3.75 4.5a2 2 0 0 1-2.78.27Z"/>
            </svg>
          </div>
          <span class="text-xl font-bold tracking-tight">
            <span class="text-white">Restaurante</span><span class="text-primary-500">POS</span>
          </span>
        </a>
        <p class="text-slate-400 text-sm max-w-md mb-6">
          Sistema integral de punto de venta y gestión para restaurantes. 
          Diseñado con tecnología moderna para el mercado gastronómico peruano.
        </p>
        <!-- Tech badges -->
        <div class="flex flex-wrap gap-2">
          <span class="px-3 py-1 rounded-full bg-white/5 border border-white/10 text-xs text-slate-400">Next.js 16</span>
          <span class="px-3 py-1 rounded-full bg-white/5 border border-white/10 text-xs text-slate-400">React 19</span>
          <span class="px-3 py-1 rounded-full bg-white/5 border border-white/10 text-xs text-slate-400">PostgreSQL</span>
          <span class="px-3 py-1 rounded-full bg-white/5 border border-white/10 text-xs text-slate-400">PWA</span>
        </div>
      </div>

      <!-- Product Links -->
      <div>
        <h4 class="text-white font-semibold mb-4">Producto</h4>
        <ul class="space-y-3">
          {links.product.map(link => (
            <li>
              <a href={link.href} class="text-sm text-slate-400 hover:text-white transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Company Links -->
      <div>
        <h4 class="text-white font-semibold mb-4">Empresa</h4>
        <ul class="space-y-3">
          {links.company.map(link => (
            <li>
              <a href={link.href} class="text-sm text-slate-400 hover:text-white transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <!-- Bottom Bar -->
    <div class="pt-8 border-t border-white/10 flex flex-col md:flex-row justify-between items-center gap-4">
      <div class="text-sm text-slate-500">
        © {currentYear} RestaurantePOS. Todos los derechos reservados.
      </div>
      <div class="flex items-center gap-4">
        <a href="#" class="text-sm text-slate-400 hover:text-white transition-colors">
          Política de Privacidad
        </a>
        <a href="#" class="text-sm text-slate-400 hover:text-white transition-colors">
          Términos de Servicio
        </a>
      </div>
    </div>
  </div>
</footer>

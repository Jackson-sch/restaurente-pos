---
const plans = [
  {
    name: "Básico",
    description: "Ideal para restaurantes pequeños",
    price: "199",
    popular: false,
    features: [
      "Gestión de Pedidos",
      "Vista Cocina básica",
      "Hasta 10 mesas",
      "1 caja registradora",
      "Reportes básicos",
      "Soporte por email"
    ],
    notIncluded: [
      "Inventario avanzado",
      "Facturación SUNAT",
      "Multi-sucursal"
    ]
  },
  {
    name: "Profesional",
    description: "Para restaurantes en crecimiento",
    price: "349",
    popular: true,
    features: [
      "Todo del plan Básico",
      "Vista Cocina en tiempo real",
      "Mesas ilimitadas",
      "3 cajas registradoras",
      "Control de Inventario",
      "Facturación SUNAT",
      "Analíticas avanzadas",
      "Soporte WhatsApp 24/7"
    ],
    notIncluded: [
      "Multi-sucursal"
    ]
  },
  {
    name: "Empresarial",
    description: "Para cadenas y franquicias",
    price: "599",
    popular: false,
    features: [
      "Todo del plan Profesional",
      "Multi-sucursal",
      "Usuarios ilimitados",
      "API personalizada",
      "Reportes consolidados",
      "Onboarding dedicado",
      "Soporte prioritario",
      "Personalización de marca"
    ],
    notIncluded: []
  }
];
---

<section id="precios" class="section-padding relative">
  <div class="container-custom">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <span class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass text-sm font-medium text-accent-500 mb-4">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 2v20l2-1 2 1 2-1 2 1 2-1 2 1 2-1 2 1V2l-2 1-2-1-2 1-2-1-2 1-2-1-2 1Z"/><path d="M16 8h-6a2 2 0 1 0 0 4h4a2 2 0 1 1 0 4H8"/><path d="M12 17.5v.5m0-12v.5"/></svg>
        Planes flexibles
      </span>
      <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold mb-4">
        <span class="text-white">Precios </span>
        <span class="gradient-text">Transparentes</span>
      </h2>
      <p class="text-lg text-slate-400 max-w-2xl mx-auto">
        Elige el plan que mejor se adapte a tu negocio. 
        Todos incluyen actualizaciones y soporte.
      </p>
    </div>

    <!-- Pricing Grid -->
    <div class="grid lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
      {plans.map((plan, index) => (
        <div class={`relative glass-card p-8 ${plan.popular ? 'border-primary-500/50 glow-orange scale-105 lg:scale-110 z-10' : ''} opacity-0 animate-slide-up stagger-${index + 1}`}>
          {plan.popular && (
            <div class="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full bg-gradient-to-r from-primary-500 to-primary-600 text-sm font-medium">
              Más Popular
            </div>
          )}
          
          <div class="text-center mb-8">
            <h3 class="text-xl font-bold text-white mb-2">{plan.name}</h3>
            <p class="text-sm text-slate-400 mb-4">{plan.description}</p>
            <div class="flex items-baseline justify-center gap-1">
              <span class="text-2xl text-slate-400">S/</span>
              <span class="text-5xl font-bold gradient-text">{plan.price}</span>
              <span class="text-slate-400">/mes</span>
            </div>
          </div>

          <ul class="space-y-3 mb-8">
            {plan.features.map(feature => (
              <li class="flex items-center gap-3 text-sm">
                <div class="w-5 h-5 rounded-full bg-secondary-500/20 flex items-center justify-center flex-shrink-0">
                  <svg xmlns="http://www.w3.org/2000/svg" class="w-3 h-3 text-secondary-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
                </div>
                <span class="text-slate-300">{feature}</span>
              </li>
            ))}
            {plan.notIncluded.map(feature => (
              <li class="flex items-center gap-3 text-sm">
                <div class="w-5 h-5 rounded-full bg-slate-800 flex items-center justify-center flex-shrink-0">
                  <svg xmlns="http://www.w3.org/2000/svg" class="w-3 h-3 text-slate-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 6 6 18"/><path d="m6 6 12 12"/></svg>
                </div>
                <span class="text-slate-500">{feature}</span>
              </li>
            ))}
          </ul>

          <a 
            href={`https://wa.me/57934835062?text=Hola%2C%20me%20interesa%20el%20plan%20${plan.name}%20de%20RestaurantePOS`}
            target="_blank"
            rel="noopener noreferrer"
            class={`block text-center w-full py-3 rounded-full font-medium transition-all ${plan.popular ? 'btn-primary' : 'btn-secondary'}`}
          >
            Empezar Ahora
          </a>
        </div>
      ))}
    </div>

    <!-- Money back guarantee -->
    <div class="text-center mt-12">
      <div class="inline-flex items-center gap-3 glass rounded-xl px-6 py-4">
        <div class="w-10 h-10 rounded-full bg-secondary-500/20 flex items-center justify-center">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-secondary-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10"/><path d="m9 12 2 2 4-4"/></svg>
        </div>
        <div class="text-left">
          <div class="text-white font-medium">30 días de garantía</div>
          <div class="text-sm text-slate-400">Si no estás satisfecho, te devolvemos tu dinero</div>
        </div>
      </div>
    </div>
  </div>
</section>
